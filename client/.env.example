# Copiar a .env y ajustar según el entorno.
# En Railway: setear VITE_PAYMENTS_ENABLED como Build Variable (no Runtime).

# Cambiar a true cuando MercadoPago esté integrado.
VITE_PAYMENTS_ENABLED=false
